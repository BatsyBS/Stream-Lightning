# Stream-Lightning
stream-lightning/
│
├── server.py                 # Main server file
├── requirements.txt          # Python dependencies
│
├── templates/
│   ├── index.html           # Home page
│   ├── stream.html          # Broadcaster page
│   └── watch.html           # Viewer page
│
└── static/
    └── js/
        ├── broadcaster.js   # Broadcaster logic
        └── viewer.js        # Viewer logic

        install python 
        pip install -r requirements.txt (use this in cmd in this file terminal ) if you start through file terminal
        # To Run the server
python server.py or python -m server

 you will recieve the host link in terminal itself.
        
